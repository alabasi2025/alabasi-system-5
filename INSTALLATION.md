# 🚀 دليل التثبيت - نظام العباسي رقم 5

## 📋 المتطلبات الأساسية

### 1. نظام التشغيل
- **Windows 10** أو أحدث (64-bit)
- **Windows Server 2016** أو أحدث

### 2. البرامج المطلوبة

| البرنامج | الإصدار المطلوب | رابط التحميل |
|---------|-----------------|--------------|
| **Node.js** | 18.0.0 أو أحدث | [nodejs.org](https://nodejs.org/) |
| **Git** | 2.0.0 أو أحدث | [git-scm.com](https://git-scm.com/) |
| **MySQL** | 8.0 أو أحدث | [mysql.com](https://dev.mysql.com/downloads/mysql/) |
| **NSSM** | 2.24 أو أحدث | [nssm.cc](https://nssm.cc/download) |

### 3. متطلبات النظام

- **المعالج**: Intel Core i3 أو أفضل
- **الذاكرة**: 4 GB RAM (موصى به 8 GB)
- **المساحة**: 5 GB مساحة فارغة
- **الشبكة**: اتصال إنترنت نشط

---

## 🎯 طريقة التثبيت السريع (موصى بها)

### الخطوة 1: تحميل المشروع

```bash
git clone https://github.com/alabasi2025/alabasi-system-5.git
cd alabasi-system-5
```

### الخطوة 2: تشغيل المثبت

1. افتح مجلد `installer/`
2. انقر نقراً مزدوجاً على `Install.bat`
3. اتبع التعليمات على الشاشة

### الخطوة 3: إدخال المعلومات

سيطلب منك المثبت:

1. **رابط قاعدة البيانات** (مثال: `mysql://root:password@localhost:3306/accounting_system`)
2. **رقم المنفذ** (افتراضي: 3000)
3. **اسم النظام** (افتراضي: نظام العباسي رقم 5)

### الخطوة 4: انتظر الإكمال

المثبت سيقوم بـ:
- ✅ فحص المتطلبات
- ✅ تثبيت الاعتماديات (pnpm install)
- ✅ إعداد قاعدة البيانات (pnpm db:push)
- ✅ تشغيل النظام كخدمة Windows
- ✅ فتح المتصفح تلقائياً

### الخطوة 5: إضافة بيانات تجريبية (اختياري)

لتسهيل الاختبار الفوري:

```bash
pnpm db:seed
```

هذا الأمر يضيف:
- 3 عملات (ريال، دولار، يورو)
- 2 فرع
- 30+ حساب في دليل الحسابات
- 9 حسابات تحليلية
- 3 موظفين
- 3 منتجات

للتفاصيل الكاملة، راجع [SEED_DATA.md](SEED_DATA.md)

---

## 🔧 طريقة التثبيت اليدوي

### 1. تثبيت Node.js و pnpm

```bash
# تحميل Node.js من الموقع الرسمي
# ثم تثبيت pnpm
npm install -g pnpm
```

### 2. تثبيت Git

```bash
# تحميل Git من الموقع الرسمي وتثبيته
```

### 3. استنساخ المشروع

```bash
git clone https://github.com/alabasi2025/alabasi-system-5.git
cd alabasi-system-5
```

### 4. تثبيت الاعتماديات

```bash
pnpm install
```

### 5. إعداد قاعدة البيانات

#### إنشاء قاعدة البيانات

```sql
CREATE DATABASE accounting_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

#### تطبيق الجداول

```bash
pnpm db:push
```

### 6. إعداد المتغيرات البيئية

أنشئ ملف `.env` في المجلد الرئيسي:

```env
DATABASE_URL=mysql://root:password@localhost:3306/accounting_system
PORT=3000
NODE_ENV=production
JWT_SECRET=your-secret-key-here
VITE_APP_TITLE=نظام العباسي رقم 5
```

### 7. تشغيل النظام

#### وضع التطوير

```bash
pnpm dev
```

#### وضع الإنتاج

```bash
pnpm build
pnpm start
```

---

## 🔄 التحديث

### التحديث التلقائي

1. افتح مجلد `installer/`
2. انقر نقراً مزدوجاً على `Update.bat`
3. انتظر إكمال التحديث

### التحديث اليدوي

```bash
cd alabasi-system-5
git pull origin main
pnpm install
pnpm db:push
pnpm build
```

---

## 🛠️ إدارة الخدمة (Windows Service)

### عرض حالة الخدمة

```powershell
sc query AlabasiSystem5
```

### إيقاف الخدمة

```powershell
sc stop AlabasiSystem5
```

### تشغيل الخدمة

```powershell
sc start AlabasiSystem5
```

### إعادة تشغيل الخدمة

```powershell
sc stop AlabasiSystem5
sc start AlabasiSystem5
```

### حذف الخدمة

```powershell
sc delete AlabasiSystem5
```

---

## 🌐 الوصول إلى النظام

بعد التثبيت الناجح:

1. افتح المتصفح
2. اذهب إلى: `http://localhost:3000`
3. سجل الدخول باستخدام Manus OAuth

---

## 🔍 حل المشاكل الشائعة

### المشكلة 1: خطأ في الاتصال بقاعدة البيانات

**الحل:**
```bash
# تأكد من تشغيل MySQL
# تحقق من صحة رابط الاتصال في .env
# تأكد من وجود قاعدة البيانات
```

### المشكلة 2: المنفذ 3000 مستخدم

**الحل:**
```bash
# غيّر المنفذ في ملف .env
PORT=3001
```

### المشكلة 3: pnpm غير موجود

**الحل:**
```bash
npm install -g pnpm
```

### المشكلة 4: خطأ في الصلاحيات

**الحل:**
```bash
# شغّل PowerShell كمسؤول
# أو غيّر سياسة التنفيذ:
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### المشكلة 5: الخدمة لا تبدأ تلقائياً

**الحل:**
```powershell
# تحقق من سجلات الخدمة
Get-EventLog -LogName Application -Source AlabasiSystem5 -Newest 10

# أعد تثبيت الخدمة
.\installer\Install.bat
```

---

## 📊 قاعدة البيانات

### الجداول (18 جدول)

1. **users** - المستخدمون
2. **roles** - الأدوار
3. **permissions** - الصلاحيات
4. **userRoles** - ربط المستخدمين بالأدوار
5. **currencies** - العملات
6. **branches** - الفروع
7. **units** - الوحدات
8. **organizations** - المؤسسات
9. **accountCategories** - تصنيفات الحسابات
10. **chartOfAccounts** - دليل الحسابات
11. **accountCurrencies** - عملات الحسابات
12. **analyticalAccountTypes** - أنواع الحسابات التحليلية
13. **analyticalAccounts** - الحسابات التحليلية
14. **clearingAccounts** - الحسابات الوسيطة
15. **vouchers** - السندات
16. **journalEntries** - القيود اليومية
17. **journalEntryLines** - أسطر القيود
18. **aiConversations** - محادثات الذكاء الاصطناعي

### النسخ الاحتياطي

```bash
# نسخ احتياطي يدوي
mysqldump -u root -p accounting_system > backup.sql

# استعادة من نسخة احتياطية
mysql -u root -p accounting_system < backup.sql
```

---

## 🔐 الأمان

### توصيات الأمان

1. **غيّر JWT_SECRET** إلى قيمة عشوائية قوية
2. **استخدم كلمات مرور قوية** لقاعدة البيانات
3. **فعّل HTTPS** في الإنتاج
4. **قيّد الوصول** إلى المنفذ 3000
5. **حدّث النظام** بانتظام

### Firewall

```powershell
# السماح بالمنفذ 3000
New-NetFirewallRule -DisplayName "Alabasi System 5" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```

---

## 📞 الدعم

### الموارد

- **GitHub**: https://github.com/alabasi2025/alabasi-system-5
- **التوثيق**: [DOCUMENTATION.md](DOCUMENTATION.md)
- **Issues**: https://github.com/alabasi2025/alabasi-system-5/issues

### الإبلاغ عن مشكلة

1. افتح Issue جديد على GitHub
2. أرفق سجلات الأخطاء
3. صف المشكلة بالتفصيل
4. أرفق لقطة شاشة إن أمكن

---

## ✅ قائمة التحقق بعد التثبيت

- [ ] النظام يعمل على `http://localhost:3000`
- [ ] قاعدة البيانات متصلة بنجاح
- [ ] جميع الجداول (18 جدول) موجودة
- [ ] يمكن تسجيل الدخول عبر Manus OAuth
- [ ] الخدمة تعمل كخدمة Windows
- [ ] النسخ الاحتياطي التلقائي مفعّل
- [ ] التحديثات التلقائية مفعّلة

---

**نظام العباسي رقم 5** - تثبيت سهل، أداء قوي، نتائج مضمونة! 🚀
