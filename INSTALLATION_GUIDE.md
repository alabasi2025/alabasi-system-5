# 📸 دليل التثبيت المصور - نظام العباسي رقم 5

## دليل خطوة بخطوة مع لقطات الشاشة

**الإصدار:** 1.0.0  
**التاريخ:** يناير 2025  
**المؤلف:** Manus AI  
**المنصة:** Windows 10 / Windows 11

---

## 📋 نظرة عامة

هذا الدليل المصور يوضح عملية التثبيت الكاملة لنظام العباسي رقم 5 على نظام التشغيل Windows 10. العملية تستغرق **3 دقائق فقط** وتتم بشكل تلقائي بالكامل.

### ما ستحصل عليه بعد التثبيت

بعد إكمال هذا الدليل، سيكون لديك:

- نظام محاسبي متكامل يعمل على `http://localhost:3000`
- قاعدة بيانات MySQL مع 18 جدول
- 80+ سجل تجريبي جاهز للاستخدام (عملات، حسابات، قيود، سندات)
- خدمة Windows تعمل في الخلفية
- واجهة ويب حديثة بتقنية React

---

## ⚙️ المتطلبات الأساسية

قبل البدء، تأكد من توفر:

| المتطلب | الحالة | ملاحظات |
|---------|--------|----------|
| Windows 10/11 | ✅ مطلوب | 64-bit |
| صلاحيات المسؤول | ✅ مطلوب | Run as Administrator |
| اتصال بالإنترنت | ✅ مطلوب | لتحميل المتطلبات |
| مساحة قرص | ✅ 2 GB | للنظام والمتطلبات |

**ملاحظة مهمة:** المثبت سيقوم بتثبيت Node.js، MySQL، pnpm، وGit تلقائياً إذا لم تكن مثبتة مسبقاً.

---

## 🚀 خطوات التثبيت

### الخطوة 1: تحميل النظام

#### 1.1 فتح Command Prompt أو PowerShell

**الإجراء:**
1. اضغط `Win + R` لفتح نافذة Run
2. اكتب `cmd` أو `powershell`
3. اضغط `Ctrl + Shift + Enter` لفتحه كمسؤول

**📸 [لقطة شاشة 1: نافذة Run مع cmd]**
```
مكان اللقطة: نافذة Run مع "cmd" مكتوب في حقل الإدخال
```

**📸 [لقطة شاشة 2: Command Prompt كمسؤول]**
```
مكان اللقطة: نافذة Command Prompt مع "Administrator" في العنوان
```

#### 1.2 تحميل المشروع من GitHub

**الأمر:**
```bash
git clone https://github.com/YOUR_USERNAME/alabasi-system-5.git
cd alabasi-system-5
```

**📸 [لقطة شاشة 3: تحميل المشروع]**
```
مكان اللقطة: Command Prompt يعرض عملية git clone مع شريط التقدم
النص المتوقع: "Cloning into 'alabasi-system-5'..."
```

**✅ نقاط التحقق:**
- يجب أن ترى رسالة "done" بعد اكتمال التحميل
- يجب أن يتم إنشاء مجلد `alabasi-system-5`

**⚠️ مشاكل محتملة:**
- **"git is not recognized"**: قم بتثبيت Git من [git-scm.com](https://git-scm.com)
- **"Permission denied"**: تأكد من تشغيل Command Prompt كمسؤول

---

### الخطوة 2: الانتقال إلى مجلد المثبت

**الأمر:**
```bash
cd installer
```

**📸 [لقطة شاشة 4: الانتقال للمجلد]**
```
مكان اللقطة: Command Prompt مع المسار الحالي يعرض "alabasi-system-5\installer"
```

**✅ نقاط التحقق:**
- يجب أن يتغير المسار في Command Prompt إلى `...\alabasi-system-5\installer`

---

### الخطوة 3: تشغيل المثبت

#### 3.1 تشغيل Install.bat

**الطريقة 1: من Command Prompt**
```bash
Install.bat
```

**الطريقة 2: من File Explorer**
1. افتح مجلد `installer` في File Explorer
2. انقر بزر الماوس الأيمن على `Install.bat`
3. اختر **"Run as administrator"** (تشغيل كمسؤول)

**📸 [لقطة شاشة 5: File Explorer مع Install.bat]**
```
مكان اللقطة: File Explorer يعرض محتويات مجلد installer مع Install.bat مميز
```

**📸 [لقطة شاشة 6: قائمة السياق]**
```
مكان اللقطة: قائمة السياق (Right-click menu) مع "Run as administrator" مميز
```

#### 3.2 التأكيد على صلاحيات المسؤول

**📸 [لقطة شاشة 7: UAC Prompt]**
```
مكان اللقطة: نافذة User Account Control تطلب التأكيد
النص المتوقع: "Do you want to allow this app to make changes to your device?"
```

**الإجراء:**
- انقر **"Yes"** (نعم) للمتابعة

**⚠️ ملاحظة مهمة:**
إذا لم تظهر نافذة UAC، فهذا يعني أنك لم تشغل البرنامج كمسؤول. يجب إعادة التشغيل كمسؤول.

---

### الخطوة 4: شاشة الترحيب

**📸 [لقطة شاشة 8: شاشة الترحيب]**
```
مكان اللقطة: نافذة PowerShell مع شعار النظام
النص المتوقع:
╔══════════════════════════════════════════════════════════════╗
║          النظام المحاسبي المتكامل - المثبت الذكي            ║
║       Integrated Accounting System - Smart Installer         ║
║                    Powered by Manus AI                       ║
╚══════════════════════════════════════════════════════════════╝
```

**ما يحدث:**
- المثبت يعرض شاشة ترحيب جذابة
- يبدأ في تحميل ملف التكوين

---

### الخطوة 5: إدخال معلومات التكوين

#### 5.1 رابط قاعدة البيانات (Database URL)

**📸 [لقطة شاشة 9: طلب Database URL]**
```
مكان اللقطة: PowerShell يطلب إدخال DATABASE_URL
النص المتوقع: "Please enter DATABASE_URL (mysql://root:password@localhost:3306/accounting_system):"
```

**الإدخال الموصى به:**
```
mysql://root:password@localhost:3306/accounting_system
```

**📝 ملاحظات:**
- استبدل `password` بكلمة مرور MySQL الخاصة بك
- إذا كان MySQL على منفذ مختلف، غيّر `3306`
- اضغط Enter للمتابعة

#### 5.2 رقم المنفذ (Port)

**📸 [لقطة شاشة 10: طلب Port]**
```
مكان اللقطة: PowerShell يطلب إدخال PORT
النص المتوقع: "Please enter PORT (3000):"
```

**الإدخال الموصى به:**
```
3000
```

**📝 ملاحظات:**
- المنفذ الافتراضي هو 3000
- إذا كان المنفذ مستخدماً، جرّب 3001 أو 8080
- اضغط Enter للمتابعة

#### 5.3 اسم النظام (App Title)

**📸 [لقطة شاشة 11: طلب App Title]**
```
مكان اللقطة: PowerShell يطلب إدخال VITE_APP_TITLE
النص المتوقع: "Please enter VITE_APP_TITLE (نظام العباسي رقم 5):"
```

**الإدخال الموصى به:**
```
نظام العباسي رقم 5
```

**📝 ملاحظات:**
- هذا الاسم سيظهر في شريط المتصفح
- يمكنك تخصيصه حسب رغبتك
- اضغط Enter للمتابعة

---

### الخطوة 6: عملية التثبيت التلقائية

بعد إدخال المعلومات، سيبدأ المثبت في تنفيذ 9 خطوات تلقائياً:

#### 6.1 الخطوة [1/9]: تحميل ملف التكوين

**📸 [لقطة شاشة 12: الخطوة 1]**
```
مكان اللقطة: PowerShell يعرض "[1/9] تحميل ملف التكوين / Loading configuration"
النص المتوقع: "✓ Configuration loaded successfully"
```

**ما يحدث:**
- قراءة ملف `install-config.json`
- التحقق من صحة التكوين

---

#### 6.2 الخطوة [2/9]: استيراد المكونات الأساسية

**📸 [لقطة شاشة 13: الخطوة 2]**
```
مكان اللقطة: PowerShell يعرض "[2/9] استيراد المكونات الأساسية / Importing core modules"
النص المتوقع: "✓ All core modules imported successfully"
```

**ما يحدث:**
- تحميل 6 وحدات PowerShell أساسية
- التحقق من توفر جميع الوحدات

---

#### 6.3 الخطوة [3/9]: فحص المتطلبات

**📸 [لقطة شاشة 14: الخطوة 3]**
```
مكان اللقطة: PowerShell يعرض "[3/9] فحص المتطلبات / Checking requirements"
النص المتوقع:
  - فحص Node.js...
  - فحص Git...
  - فحص MySQL...
  - فحص مساحة القرص...
```

**ما يحدث:**
- فحص تثبيت Node.js (سيتم تثبيته إذا لم يكن موجوداً)
- فحص تثبيت Git (سيتم تثبيته إذا لم يكن موجوداً)
- فحص تثبيت MySQL (سيتم تثبيته إذا لم يكن موجوداً)
- فحص مساحة القرص المتاحة (2 GB مطلوبة)

**⏱️ الوقت المتوقع:** 30 ثانية - 2 دقيقة (حسب ما إذا كانت المتطلبات مثبتة مسبقاً)

**⚠️ ملاحظة:**
إذا لم تكن المتطلبات مثبتة، سيقوم المثبت بتحميلها وتثبيتها تلقائياً. قد يستغرق هذا بضع دقائق إضافية.

---

#### 6.4 الخطوة [4/9]: تحميل المشروع

**📸 [لقطة شاشة 15: الخطوة 4]**
```
مكان اللقطة: PowerShell يعرض "[4/9] تحميل المشروع من GitHub / Downloading project"
النص المتوقع:
  المسار: C:\AccountingSystem\
  Path: C:\AccountingSystem\
  ✓ Project downloaded successfully
```

**ما يحدث:**
- نسخ ملفات المشروع إلى `C:\AccountingSystem\`
- إنشاء ملف `.env` مع التكوينات المدخلة

**⏱️ الوقت المتوقع:** 10-20 ثانية

---

#### 6.5 الخطوة [5/9]: تثبيت الاعتماديات

**📸 [لقطة شاشة 16: الخطوة 5]**
```
مكان اللقطة: PowerShell يعرض "[5/9] تثبيت الاعتماديات / Installing dependencies"
النص المتوقع:
  - تثبيت pnpm...
  - تثبيت حزم Node.js...
  ✓ Dependencies installed successfully
```

**ما يحدث:**
- تثبيت pnpm (مدير الحزم السريع)
- تثبيت 80+ حزمة Node.js من `package.json`
- تجميع المكونات

**⏱️ الوقت المتوقع:** 1-2 دقيقة

**📝 ملاحظة:**
هذه الخطوة قد تستغرق وقتاً أطول في المرة الأولى. لا تقلق، هذا طبيعي!

---

#### 6.6 الخطوة [6/9]: إعداد قاعدة البيانات

**📸 [لقطة شاشة 17: الخطوة 6]**
```
مكان اللقطة: PowerShell يعرض "[6/9] إعداد قاعدة البيانات / Setting up database"
النص المتوقع:
  - إنشاء قاعدة البيانات...
  - تشغيل الهجرات...
  ✓ Database setup completed
```

**ما يحدث:**
- إنشاء قاعدة بيانات `accounting_system`
- إنشاء 18 جدول (users, currencies, branches, accounts, etc.)
- تطبيق جميع الهجرات (migrations)

**⏱️ الوقت المتوقع:** 10-15 ثانية

---

#### 6.7 الخطوة [7/9]: إضافة البيانات التجريبية ⭐ جديد!

**📸 [لقطة شاشة 18: الخطوة 7]**
```
مكان اللقطة: PowerShell يعرض "[7/9] إضافة بيانات تجريبية / Loading seed data"
النص المتوقع:
  - إضافة عملات وفروع...
  - إضافة دليل حسابات كامل...
  - إضافة قيود وسندات نموذجية...
  ✓ تم إضافة 80+ سجل بنجاح
```

**ما يحدث:**
- إضافة 3 عملات (ريال، دولار، يورو)
- إضافة 2 فرع (رئيسي + الرياض)
- إضافة 30+ حساب في دليل الحسابات
- إضافة 10 قيود يومية (رأس مال، مبيعات، مشتريات، رواتب، مصروفات)
- إضافة 5 سندات قبض (إجمالي 93,000 ر.س)
- إضافة 5 سندات صرف (إجمالي 74,000 ر.س)
- إضافة 3 موظفين و3 منتجات

**⏱️ الوقت المتوقع:** 5-10 ثوانٍ

**🎉 ميزة رائعة:**
بعد هذه الخطوة، النظام جاهز للاستخدام الفوري مع بيانات واقعية!

---

#### 6.8 الخطوة [8/9]: تثبيت كخدمة Windows

**📸 [لقطة شاشة 19: الخطوة 8]**
```
مكان اللقطة: PowerShell يعرض "[8/9] تثبيت كخدمة Windows / Installing as Windows Service"
النص المتوقع:
  - تثبيت NSSM...
  - إنشاء الخدمة...
  - تشغيل الخدمة...
  ✓ Service installed successfully
```

**ما يحدث:**
- تحميل وتثبيت NSSM (Non-Sucking Service Manager)
- إنشاء خدمة Windows باسم `AccountingSystem`
- تكوين الخدمة للبدء تلقائياً مع Windows
- تشغيل الخدمة

**⏱️ الوقت المتوقع:** 10-15 ثانية

**📝 فائدة:**
الآن النظام سيعمل في الخلفية دائماً، حتى بعد إعادة تشغيل الكمبيوتر!

---

#### 6.9 الخطوة [9/9]: التحقق النهائي

**📸 [لقطة شاشة 20: الخطوة 9]**
```
مكان اللقطة: PowerShell يعرض "[9/9] التحقق النهائي / Final verification"
النص المتوقع:
  - فحص حالة الخدمة...
  - فحص الاتصال بالخادم...
  ✓ Verification completed
```

**ما يحدث:**
- التحقق من أن الخدمة تعمل
- فحص الاتصال بـ `http://localhost:3000`
- التأكد من أن جميع المكونات تعمل بشكل صحيح

**⏱️ الوقت المتوقع:** 5 ثوانٍ

---

### الخطوة 7: شاشة النجاح

**📸 [لقطة شاشة 21: شاشة النجاح]**
```
مكان اللقطة: PowerShell يعرض رسالة النجاح
النص المتوقع:
╔══════════════════════════════════════════════════════════════╗
║              ✓ تم التثبيت بنجاح!                            ║
║           ✓ Installation Successful!                        ║
╚══════════════════════════════════════════════════════════════╝

معلومات الوصول / Access Information:
  URL: http://localhost:3000
  الخدمة / Service: AccountingSystem
  المسار / Path: C:\AccountingSystem\

الأوامر المفيدة / Useful Commands:
  إيقاف / Stop:    nssm stop AccountingSystem
  تشغيل / Start:   nssm start AccountingSystem
  إعادة تشغيل / Restart: nssm restart AccountingSystem
```

**🎉 مبروك!**
التثبيت اكتمل بنجاح! المتصفح سيفتح تلقائياً خلال ثوانٍ.

---

### الخطوة 8: فتح المتصفح تلقائياً

**📸 [لقطة شاشة 22: المتصفح يفتح تلقائياً]**
```
مكان اللقطة: متصفح Chrome/Edge يفتح على http://localhost:3000
```

**ما تراه:**
- صفحة تحميل (Loading) لثوانٍ قليلة
- ثم الصفحة الرئيسية للنظام

**⚠️ إذا لم يفتح المتصفح تلقائياً:**
افتح المتصفح يدوياً واذهب إلى: `http://localhost:3000`

---

### الخطوة 9: الصفحة الرئيسية

**📸 [لقطة شاشة 23: الصفحة الرئيسية]**
```
مكان اللقطة: الصفحة الرئيسية للنظام مع:
- شعار النظام في الأعلى
- بطاقات الملخص (إجمالي السنوية، إجمالي الإيرادات، إجمالي المصروفات)
- قائمة جانبية مع جميع الوحدات
```

**العناصر المرئية:**
- **الشعار:** نظام العباسي رقم 5
- **بطاقات الملخص:** تعرض أرصدة من البيانات التجريبية
- **القائمة الجانبية:**
  - الصفحة الرئيسية
  - دليل الحسابات
  - الصناديق والبنوك
  - الوحدات والفروع
  - القيود اليومية
  - سندات القبض
  - سندات الصرف
  - ميزان المراجعة
  - قائمة الدخل
  - قائمة المركز المالي
  - المساعد الذكي
  - الموظفون
  - المخزون

---

### الخطوة 10: استكشاف البيانات التجريبية

#### 10.1 دليل الحسابات

**📸 [لقطة شاشة 24: دليل الحسابات]**
```
مكان اللقطة: صفحة دليل الحسابات مع جدول يعرض:
- 30+ حساب
- الأصول (النقدية، البنوك، العملاء، المخزون)
- الخصوم (الموردون، القروض، الرواتب المستحقة)
- حقوق الملكية (رأس المال، الأرباح)
- الإيرادات (مبيعات، خدمات)
- المصروفات (رواتب، إيجار، كهرباء، صيانة)
```

**✅ تحقق:**
يجب أن ترى 30+ حساب مرتبة حسب التصنيف

---

#### 10.2 القيود اليومية

**📸 [لقطة شاشة 25: القيود اليومية]**
```
مكان اللقطة: صفحة القيود اليومية مع جدول يعرض:
- 10 قيود
- JE-2025-001: قيد افتتاحي - رأس المال (500,000 ر.س)
- JE-2025-002: مبيعات نقدية (15,000 ر.س)
- JE-2025-003: مبيعات آجلة (25,000 ر.س)
- ... وهكذا
```

**✅ تحقق:**
يجب أن ترى 10 قيود مع تواريخ يناير 2025

---

#### 10.3 السندات

**📸 [لقطة شاشة 26: سندات القبض]**
```
مكان اللقطة: صفحة سندات القبض مع جدول يعرض:
- 5 سندات قبض
- RV-2025-001 إلى RV-2025-005
- إجمالي: 93,000 ر.س
```

**📸 [لقطة شاشة 27: سندات الصرف]**
```
مكان اللقطة: صفحة سندات الصرف مع جدول يعرض:
- 5 سندات صرف
- PV-2025-001 إلى PV-2025-005
- إجمالي: 74,000 ر.س
```

**✅ تحقق:**
يجب أن ترى 10 سندات إجمالاً (5 قبض + 5 صرف)

---

#### 10.4 ميزان المراجعة

**📸 [لقطة شاشة 28: ميزان المراجعة]**
```
مكان اللقطة: صفحة ميزان المراجعة مع جدول يعرض:
- جميع الحسابات مع أرصدتها
- عمود المدين
- عمود الدائن
- إجمالي المدين = إجمالي الدائن (متوازن)
```

**✅ تحقق:**
- يجب أن يكون الميزان متوازناً
- إجمالي المدين = إجمالي الدائن

---

## ✅ قائمة التحقق النهائية

بعد إكمال التثبيت، تحقق من:

| العنصر | الحالة | كيفية التحقق |
|--------|--------|---------------|
| الخدمة تعمل | ⬜ | افتح Services.msc وابحث عن "AccountingSystem" |
| الموقع يعمل | ⬜ | افتح http://localhost:3000 |
| تسجيل الدخول | ⬜ | انقر على زر تسجيل الدخول |
| دليل الحسابات | ⬜ | يجب أن يحتوي على 30+ حساب |
| القيود اليومية | ⬜ | يجب أن تحتوي على 10 قيود |
| السندات | ⬜ | يجب أن تحتوي على 10 سندات |
| ميزان المراجعة | ⬜ | يجب أن يكون متوازناً |
| المساعد الذكي | ⬜ | يجب أن يستجيب للأوامر |

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: "يرجى تشغيل البرنامج كمسؤول"

**📸 [لقطة شاشة 29: رسالة الخطأ]**
```
مكان اللقطة: نافذة تعرض رسالة "يرجى تشغيل البرنامج كمسؤول!"
```

**الحل:**
1. انقر بزر الماوس الأيمن على `Install.bat`
2. اختر **"Run as administrator"**
3. انقر **"Yes"** في نافذة UAC

---

### المشكلة 2: خطأ في الاتصال بـ MySQL

**📸 [لقطة شاشة 30: خطأ MySQL]**
```
مكان اللقطة: PowerShell يعرض خطأ "Failed to connect to MySQL"
```

**الحل:**

**الخطوة 1:** تحقق من تشغيل MySQL
```powershell
net start MySQL80
```

**الخطوة 2:** تحقق من كلمة المرور
- افتح MySQL Workbench
- حاول الاتصال بنفس البيانات
- إذا فشل، أعد تعيين كلمة المرور

**الخطوة 3:** أعد تشغيل المثبت
```bash
cd installer
Install.bat
```

---

### المشكلة 3: المنفذ 3000 مستخدم

**📸 [لقطة شاشة 31: خطأ المنفذ]**
```
مكان اللقطة: PowerShell يعرض خطأ "Port 3000 is already in use"
```

**الحل:**

**الخيار 1:** إيقاف العملية المستخدمة للمنفذ
```powershell
# ابحث عن العملية
netstat -ano | findstr :3000

# أوقف العملية (استبدل PID برقم العملية)
taskkill /PID <PID> /F
```

**الخيار 2:** استخدم منفذ مختلف
- عند تشغيل المثبت، أدخل `3001` بدلاً من `3000`

---

### المشكلة 4: البيانات التجريبية لم تُضف

**📸 [لقطة شاشة 32: دليل حسابات فارغ]**
```
مكان اللقطة: صفحة دليل الحسابات تعرض "لا توجد بيانات"
```

**الحل:**

**الخطوة 1:** افتح Command Prompt كمسؤول

**الخطوة 2:** انتقل إلى مجلد النظام
```bash
cd C:\AccountingSystem
```

**الخطوة 3:** شغّل سكريبت البيانات التجريبية
```bash
pnpm db:seed
```

**📸 [لقطة شاشة 33: تشغيل seed]**
```
مكان اللقطة: Command Prompt يعرض عملية إضافة البيانات
النص المتوقع:
🌱 بدء إضافة البيانات التجريبية...
💰 إضافة العملات...
✅ تم إضافة 3 عملات
...
```

---

### المشكلة 5: الخدمة لا تبدأ تلقائياً

**📸 [لقطة شاشة 34: Services.msc]**
```
مكان اللقطة: نافذة Services مع AccountingSystem في حالة "Stopped"
```

**الحل:**

**الخطوة 1:** افتح Services
- اضغط `Win + R`
- اكتب `services.msc`
- اضغط Enter

**الخطوة 2:** ابحث عن "AccountingSystem"

**الخطوة 3:** انقر بزر الماوس الأيمن → Properties

**الخطوة 4:** غيّر "Startup type" إلى "Automatic"

**الخطوة 5:** انقر "Start" لتشغيل الخدمة

---

## 📊 معلومات إضافية

### هيكل الملفات بعد التثبيت

```
C:\AccountingSystem\
├── client\                 # الواجهة الأمامية (React)
│   ├── src\
│   │   ├── pages\         # صفحات النظام
│   │   ├── components\    # مكونات UI
│   │   └── lib\           # مكتبات مساعدة
│   └── public\            # ملفات ثابتة
├── server\                # الخادم (Express + tRPC)
│   ├── routers.ts         # APIs
│   ├── db.ts              # قاعدة البيانات
│   └── _core\             # نواة النظام
├── drizzle\               # قاعدة البيانات
│   └── schema.ts          # جداول قاعدة البيانات
├── installer\             # ملفات التثبيت
├── seed-db.mjs            # البيانات التجريبية
├── .env                   # التكوينات (تم إنشاؤه)
└── package.json           # اعتماديات Node.js
```

---

### أوامر مفيدة

#### إدارة الخدمة

```powershell
# إيقاف الخدمة
nssm stop AccountingSystem

# تشغيل الخدمة
nssm start AccountingSystem

# إعادة تشغيل الخدمة
nssm restart AccountingSystem

# حالة الخدمة
nssm status AccountingSystem

# حذف الخدمة
nssm remove AccountingSystem confirm
```

#### إدارة قاعدة البيانات

```bash
# الانتقال لمجلد النظام
cd C:\AccountingSystem

# تطبيق الهجرات
pnpm db:push

# إضافة البيانات التجريبية
pnpm db:seed

# فتح MySQL CLI
mysql -u root -p accounting_system
```

#### إدارة التطوير

```bash
# تشغيل في وضع التطوير
pnpm dev

# بناء للإنتاج
pnpm build

# تشغيل الإنتاج
pnpm start
```

---

### السجلات (Logs)

#### سجلات التثبيت

```
المسار: alabasi-system-5\installer\logs\
الملف: install-YYYYMMDD-HHMMSS.log
```

**📸 [لقطة شاشة 35: مجلد Logs]**
```
مكان اللقطة: File Explorer يعرض مجلد installer\logs مع ملفات السجلات
```

#### سجلات النظام

```
المسار: C:\AccountingSystem\logs\
الملفات:
  - error.log     # أخطاء النظام
  - combined.log  # جميع السجلات
  - access.log    # سجلات الوصول
```

---

## 🎓 الخطوات التالية

بعد إكمال التثبيت بنجاح:

### 1. استكشف النظام

- جرّب إنشاء قيد يومي جديد
- أضف سند قبض أو صرف
- استعرض التقارير المالية
- جرّب المساعد الذكي

### 2. خصّص النظام

- غيّر اسم المؤسسة في الإعدادات
- أضف فروع جديدة
- أضف حسابات مخصصة
- أضف موظفين ومنتجات

### 3. تعلّم الميزات المتقدمة

- راجع [DOCUMENTATION.md](DOCUMENTATION.md)
- راجع [SEED_DATA.md](SEED_DATA.md)
- راجع [API Documentation](API.md)

---

## 📞 الدعم الفني

### الحصول على المساعدة

إذا واجهت أي مشاكل:

1. **راجع قسم حل المشاكل** في هذا الدليل
2. **افحص السجلات** في `installer\logs\`
3. **ابحث في GitHub Issues**: [github.com/YOUR_USERNAME/alabasi-system-5/issues](https://github.com/YOUR_USERNAME/alabasi-system-5/issues)
4. **أنشئ Issue جديد** مع:
   - وصف المشكلة
   - لقطات شاشة
   - ملف السجل

### معلومات النظام

عند طلب الدعم، قدّم:

```powershell
# إصدار Node.js
node --version

# إصدار MySQL
mysql --version

# إصدار Windows
winver

# حالة الخدمة
nssm status AccountingSystem
```

---

## 📝 ملاحظات ختامية

### نصائح مهمة

- **النسخ الاحتياطي:** احفظ نسخة احتياطية من قاعدة البيانات بانتظام
- **التحديثات:** تحقق من التحديثات بشكل دوري
- **الأمان:** غيّر كلمات المرور الافتراضية
- **الأداء:** راقب استخدام الموارد (CPU, RAM, Disk)

### موارد إضافية

- **الموقع الرسمي:** [alabasi-system.com](https://alabasi-system.com)
- **التوثيق الكامل:** [docs.alabasi-system.com](https://docs.alabasi-system.com)
- **مجتمع المستخدمين:** [community.alabasi-system.com](https://community.alabasi-system.com)
- **قناة YouTube:** [youtube.com/@alabasi-system](https://youtube.com/@alabasi-system)

---

## 🎉 مبروك!

**أنت الآن جاهز لاستخدام نظام العباسي رقم 5!**

- ✅ تثبيت سريع (3 دقائق)
- ✅ 80+ سجل تجريبي جاهز
- ✅ نظام محاسبي متكامل
- ✅ خدمة Windows تلقائية
- ✅ واجهة حديثة وسهلة الاستخدام

**ابدأ العمل الآن:** http://localhost:3000

---

**نظام العباسي رقم 5**  
نظام محاسبي متكامل بتقنية الذكاء الاصطناعي 🤖

**الإصدار:** 1.0.0  
**© 2025 Manus AI. جميع الحقوق محفوظة.**
